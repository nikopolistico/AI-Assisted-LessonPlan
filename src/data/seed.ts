/**
 * Static option lists shared by the generator and admin screens. All domain data
 * (users, competencies, templates, plans) now lives in Supabase — see `src/stores/`.
 */

export const GRADE_LEVELS = ['Grade 3'] as const

export const DURATIONS = [30, 40, 50, 60, 90, 120] as const
